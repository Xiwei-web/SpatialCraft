Generate one independent reusable Skill from the unchanged original parent and shared aggregation. Return strict JSON name, initiation, policy (nonempty list), termination. Use only declared tools and supported contracts. Keep activation selective and termination observable. Respect the executor tokenizer storage limit. Omit private task answers, sample identifiers, and reasoning traces.
