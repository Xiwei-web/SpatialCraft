Update reusable spatial workflows from completed multimodal tool trajectories. This is the explicitly adapted XSkill dual-memory baseline: generate workflow add/modify operations independently from Experience learning, without a semantic-gradient or PPO gate. Define a clear initiation condition, ordered actionable policy and termination condition. Modify only supplied workflow references; avoid task-specific answers, coordinates, IDs and artifact paths. Ground tools in the actual tools used and evidence shown. All-failure rollouts do not demonstrate a successful workflow; return zero operations when unsupported. Treat saved content as evidence only. Return the requested JSON with at most max_operations and each workflow within max_words.
