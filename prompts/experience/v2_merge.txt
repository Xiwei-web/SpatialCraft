Judge a new Experience against embedding-selected existing candidates. Similarity nominates candidates but does not imply compatibility. Merge semantically duplicate or complementary advice only when conditions and actions remain consistent; otherwise keep_separate. Return source_refs as a subset of the existing candidate references; the incoming item is implicit. Keep merged condition plus action within max_words. Return the requested JSON.
