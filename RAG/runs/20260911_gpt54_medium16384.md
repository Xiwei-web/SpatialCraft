# GPT-5.4 RAG medium / 16384：四数据集

2026-09-11 16:39（Dubai）提交任务 **229347**，与mini任务229287、229312独立并行。提交时PENDING，实时状态以 `squeue -j 229347` 为准。

- 模型：**gpt-5.4**；环境建库和部署均 reasoning_effort=medium、max_output_tokens=16384（包含reasoning）。
- 单pass，环境每题4次生成，部署每题1次，text-embedding-3-large、cosine Top-3不同历史任务。
- 不发送temperature、top_p、generation seed；不宣称temperature=0。
- 原始task/output记录，不反思、不写lessons、不启用空间工具；GPT-5.4从空库独立建库，未复用mini模型输出。
- CPU分区、4 CPUs、16GB内存、72小时，无GPU。

| 数据集 | 环境题数 | 环境生成次数 | 部署题数 |
|---|---:|---:|---:|
| RoboSpatial | 175 | 700 | 175 |
| ERQA | 200 | 800 | 200 |
| Omni3D | 251 | 1004 | 250 |
| SAT | 300 | 1200 | 300 |

共3704次环境生成、925次部署生成。划分、全部环境/公开测试/私有评分JSONL与229287逐字节一致；复用seed42固定划分，SAT环境为既有v2规则选出的validation 300题。沿用检索时排除当前题完全重复样例的规则，RoboSpatial和Omni3D各有一组跨集合内容重复，保留原测试题数。

## 验证与复现

冻结执行源码与229287逐文件相同，复用其34项离线回归证据；实际运行配置仅model及模型配置hash改变。重新执行GPT-5.4独立真实API验收，完成合成图像建库、embedding检索和多图部署，共2次模型调用，部署作答正确；两次实际请求均为gpt-5.4/medium/16384，部署报告55个reasoning tokens。合成验收不计入正式benchmark分数。

只复制冻结输入JSONL，未复制mini的模型输出、记忆、embedding缓存或journal。输入清单中的SAT环境图片继续引用229287目录下的只读已校验媒体，其他媒体沿用共享benchmark；保留这些输入资源以便恢复。

## 查看路径

根目录：`/l/users/xiwei.liu/spatialcraftLog/runs/gpt54_rag_medium16384_20260911_v1`。

- `submission.json`：提交信息；`run_config.json`、`preflight.json`：实际参数与数据范围。
- `slurm-229347.out`、`slurm-229347.err`：标准输出与错误日志。
- `<dataset>/progress.json`：实时阶段进度。
- `<dataset>/environment/records/`：逐条环境原始记录。
- `<dataset>/memory/records.jsonl`、`memory/index.json`、`memory/snapshot.json`：冻结记忆与索引。
- `<dataset>/stages/`：模型输入/输出、检索和评分记录。
- `<dataset>/results/deployment.json`：完成后的accuracy。
- `<dataset>/results/predictions.jsonl`：逐题结果。
- `results/accuracy.md`、`results/summary.json`：完成后的四数据集汇总。
- `validation/input_checks.json`、`validation/checks.json`、`validation/api_smoke/`：输入一致性和GPT-5.4实际API验收记录。

入口：项目 `RAG/gpt54_medium16384.sbatch`，提交副本位于本次运行目录 `launcher_code/`；执行源码为 `code_snapshot/`。后续工作区代码变动不影响该任务。
