# GPT-5.4-mini RAG medium / 16384：2026-09-11

已提交 Slurm **229287**（2026-09-11 16:24 Dubai）；提交时 PENDING，实时状态以 `squeue -j 229287` 为准。该任务使用独立冻结代码，不读取之后对项目源码的修改。

- 模型：gpt-5.4-mini；环境建库和部署均 reasoning_effort=medium、max_output_tokens=16384，包含 reasoning tokens。
- 环境单pass，每题4个独立输出；部署每题1次。不开空间工具，不生成反思、lessons、Experience 或 Skill。
- medium 模式不发送 temperature、top_p、seed；实际温度记录为 null，不宣称为环境0.7/部署0。
- 默认 text-embedding-3-large，cosine Top-3 不同历史任务；每题选择最早完成且非空的环境输出作代表，不按正确性挑选。
- CPU分区，4 CPUs、16GB内存、72小时；四数据集依次执行，无GPU。

| 数据集 | 环境题数 | 环境生成次数 | 部署题数 |
|---|---:|---:|---:|
| RoboSpatial | 175 | 700 | 175 |
| ERQA | 200 | 800 | 200 |
| Omni3D | 251 | 1004 | 250 |
| SAT | 300 | 1200 | 300 |

合计3704次环境生成、925次部署生成。SAT环境从validation按现有v2规则使用seed42选300题。925道部署公开输入与 `gpt54mini_direct_baseline_20260910_v1` 逐字节一致。

## 数据重复处理

实际预检查发现 RoboSpatial 和 Omni3D 各有一组跨集合内容完全相同、ID不同的题目。保持已有划分及题数，在每次检索时按 question、choices 和图片字节hash排除与当前题完全相同的历史记录；排除ID写入检索日志和逐题结果。ERQA、SAT没有发现这种重复。这不涉及GT或正确性过滤。

最初的 `gpt54mini_rag_medium16384_20260911_v1` 在离线检查阶段被重复内容检查拦截，未提交、未运行正式推理；本次使用修复后的v2独立目录。

## 验证

- 34项离线回归通过：RAG、API baseline、v2数据准备。
- Ruff和batch shell语法检查通过，冻结源代码hash复核通过。
- 独立合成图像验收完成真实模型建库、embedding检索及多图部署；共2次模型调用，部署正确。实际两次请求均为medium/16384，无采样覆盖参数；部署响应报告38个reasoning tokens。
- 该合成验收独立于正式数据集，不计入任何benchmark accuracy。
- 参数支持参考：[OpenAI GPT-5.4-mini官方模型说明](https://developers.openai.com/api/docs/models/gpt-5.4-mini)。

## 查看位置

运行根目录：`/l/users/xiwei.liu/spatialcraftLog/runs/gpt54mini_rag_medium16384_20260911_v2`。

- 提交记录：`submission.json`；配置：`run_config.json`；输入/预算检查：`preflight.json`。
- 标准输出：`slurm-229287.out`；错误日志：`slurm-229287.err`。
- 实时进度：`<dataset>/progress.json`。
- 每条环境记录：`<dataset>/environment/records/`；完整冻结库：`<dataset>/memory/records.jsonl`。
- 逐调用输入/输出及检索日志：`<dataset>/stages/`。
- 完成后的数据集accuracy：`<dataset>/results/deployment.json`。
- 完成后的总表：`results/accuracy.md`，汇总：`results/summary.json`。
- 验证证据：`validation/checks.json`、`validation/api_smoke/validation_result.json`。

恢复须使用同一冻结快照与参数，不能把当前工作区新源码混入既有运行。重新提交前先确认229287已停止，避免重复付费调用。
